# AP38-E frozen protocol: cadence-guarded weak-regime core veto

Registered 2026-09-06 before AP38 late signals or scorecards. AP37 is the frozen
leader. AP38 tests exactly one regime-routing policy; there is no threshold,
window, target, expert-subset or controller grid.

Keep the exact AP37 mature-precision gate for AP23 late-week fallback and its
unconditional silence-10 rescue. Add a separate causal precision estimate for
the frozen AP26 core. Use the same three audited causal OOS ranks and the same
support stratum: number of ranks at or above the existing 0.70 top-30 boundary.
Use the same strict label `min(y3, y5, y10, y20)`, publication-h20 maturity before
decision date minus two days, expanding history and hierarchical shrinkage 40:
overall core precision toward 0.50, global stratum toward overall, and local
currency stratum toward global.

Run one sequential router from scratch. An AP26 core decision is accepted when
its local shrunk stratum precision is at least causal overall core precision.
If not, it may be vetoed only when the router's own trailing-365 decision rate
before the row is already at least 1; when rate is below 1 the core is preserved
as a cadence guard. Rejected core rows never enter later state. AP37 fallback,
warmup 84, silence 10, weekdays and sequential max 2 per ISO week remain exact.

Select the sole fresh policy on early 2023 with unchanged joint gates; frozen
AP37 is the registered fallback. Opened 2024-2026 remains retrospective. Audit
both precision pools, support strata, maturity/counts/shrinkage, sequential
veto/rate/silence/reason/cap state, future expert/label/source corruption prefix
invariance and paired 20/50-date uncertainty.

Acceptance remains min h3/h5/h10/h20 lift above 2.4, min currency rate at least
1, zero empty complete months and max 2 signals per week. H1 is known validity
only. Historical receipt timestamps remain calendar-assumed and bank execution
is not validated.
